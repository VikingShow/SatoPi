/**
 * Re-export barrel — canonical implementation lives at ../../graph/gate-controller.
 */
export {
	GateController,
	type GateResult,
	type HumanReviewRequest,
} from "../../graph/gate-controller";
